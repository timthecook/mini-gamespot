server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:articlesdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  # Other article-service specific properties here
